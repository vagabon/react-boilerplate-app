{
  "username": {
    "required": true,
    "min": 5,
    "max": 20,
    "regexp": "^[a-zA-Z0-9_-]*$",
    "regexpError": "ERRORS:FORMAT_USERNAME"
  },
  "email": {
    "required": true,
    "email": true
  },
  "password": {
    "required": true,
    "max": 50,
    "regexp": "^(?=.*[A-Za-z])(?=.*\\d)(?=.*[-_@$!%*?&])[A-Za-z\\d-_@$!%*?&]{8,}$",
    "regexpError": "ERRORS:FORMAT_PASSWORD"
  },
  "password2": {
    "required": true,
    "max": 50,
    "regexp": "^(?=.*[A-Za-z])(?=.*\\d)(?=.*[-_@$!%*?&])[A-Za-z\\d-_@$!%*?&]{8,}$",
    "regexpError": "ERRORS:FORMAT_PASSWORD",
    "same": "password",
    "sameLabel": "ERRORS:SAME_PASSWORD"
  }
}
